package de.mm20.launcher2.services.widgets

/**
 * The host ids the launcher registers with the AppWidget service. One host
 * for everything the launcher shows (the home grid and the widget column
 * pages), so a bound id is meaningful in every place that touches the host.
 */
object AppWidgetHostIds {
    const val Home = 44203
}
